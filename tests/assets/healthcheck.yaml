framework: flask
database:
  mongo:
    mongoengine:
      lib_engine: mongoengine
  sql:
    mysql:
      lib_engine: pymysql
messaging:
  kafka: -
    lib_engine: -
cluster:
  spark:
    lib_engine: -

