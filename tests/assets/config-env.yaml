vault:
    host: ${host_vault}
    port: ${port_vault}
    scheme: ${scheme_vault}
    token: ${token_vault}
    path: ${path_vault}
consul:
    host: ${host_consul} 
    port: ${port_consul}
    scheme: ${scheme_consul}
    token: ${token_consul}
    path: ${path_consul}
logconfig:
    mode: development
    source_type: yaml_file
    source_location: ./tests/assets/logging.yaml
    default_type: yaml_file
    default_location: ./tests/assets/logging.yaml

